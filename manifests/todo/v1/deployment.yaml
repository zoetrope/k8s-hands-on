apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo
  labels:
    app.kubernetes.io/name: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: todo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: todo
    spec:
      containers:
        - name: todo
          image: todo:v1
          imagePullPolicy: IfNotPresent
          command: ["/todo"]
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
